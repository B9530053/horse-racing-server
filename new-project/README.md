# New Project

This directory contains a fresh Express.js service scaffolded for upcoming features.

## Scripts

- `npm install` – install dependencies.
- `npm run dev` – start the server with watch mode using Node.js.
- `npm start` – run the server in production mode.

## Endpoints

- `GET /health` – returns a simple JSON payload confirming that the service is running.
